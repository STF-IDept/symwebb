<form action="{{ path('webb_post_note_create', {'fleet' : fleet, 'ship' : ship}) }}" {{ form_enctype(form) }} method="POST" class="post_note_create">
    {{ form_widget(form) }}
    <div>
        <input type="submit" value="{{ 'note.submit'|trans({}, 'WebbPostBundle') }}" />
    </div>
</form>
